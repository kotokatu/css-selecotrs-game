import { BaseComponent } from '../../common/BaseComponent';

export class Pane extends BaseComponent {
    constructor(parent: HTMLElement, className: string) {
        super({ parent, className });
    }
}
